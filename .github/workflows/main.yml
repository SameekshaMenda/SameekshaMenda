name: Generate Snake Animation

# This new section is the fix!
permissions:
  contents: write

on:
  schedule:
    - cron: "0 0 * * *" # Runs every day at midnight
  workflow_dispatch: # Allows you to run it manually

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - uses: Platane/snk@v3
        with:
          github_user_name: SameekshaMenda
          # The output path here should match the one in your README
          outputs: dist/github-contribution-grid-snake.svg

          color_snake: "#BD93F9"  # Dracula purple
          color_dots: "#282A36,#383A59,#44475A,#6272A4,#F8F8F2" # Dracula theme gradient
          
      - name: Push generated file
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add .
          git commit -m "Generate Contribution Snake" || exit 0
          git push
